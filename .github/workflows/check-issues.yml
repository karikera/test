name: Check Issues
on:
  issues:
    types: [opened, edited]

jobs:
  auto_close_issues:
    runs-on: ubuntu-latest
    steps:
      - name: Automatically close issues that don't follow the issue template
        uses: karikera/issue-closer@v1.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
